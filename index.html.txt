<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>The Cacao Bloom | Sweet carts & artisanal treats</title>

  <!-- Basic SEO -->
  <meta name="description" content="The Cacao Bloom — sweet carts for weddings, communions, birthdays and artisanal treats: handcrafted chocolate bars, ginger cookies and more." />
  <meta name="keywords" content="sweet carts, chocolate artisan, ginger cookies, wedding candy cart, event desserts, artisanal chocolate" />
  <meta name="author" content="The Cacao Bloom" />

  <!-- Open Graph -->
  <meta property="og:title" content="The Cacao Bloom | Sweet carts & artisanal treats" />
  <meta property="og:description" content="Sweet carts for weddings, communions and birthdays. Handmade chocolate bars with irregular nuts, ginger cookies and more." />
  <meta property="og:image" content="https://images.unsplash.com/photo-1599785209707-27beec04c6a1?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1200" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://yourdomain.com" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="The Cacao Bloom | Sweet carts & artisanal treats" />
  <meta name="twitter:description" content="Sweet carts for weddings, communions and birthdays. Handmade chocolate bars with irregular nuts, ginger cookies and more." />
  <meta name="twitter:image" content="https://images.unsplash.com/photo-1599785209707-27beec04c6a1?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1200" />

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">

  <!-- Optional favicon -->
  <link rel="icon" href="data:,">

  <style>
    /* --- Base --- */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{
      font-family: 'Roboto', sans-serif;
      background:#fffaf5;
      color:#3f2f25;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.6;
      scroll-behavior:smooth;
    }
    a{color:inherit;text-decoration:none}

    /* --- Header --- */
    header{
      background: url('https://images.unsplash.com/photo-1599785209707-27beec04c6a1?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1600') center/cover no-repeat;
      height:72vh;
      display:flex;align-items:center;justify-content:center;text-align:center;position:relative;color:#fff;
    }
    header::after{
      content:'';position:absolute;inset:0;background:linear-gradient(180deg, rgba(0,0,0,.35), rgba(0,0,0,.45));
    }
    .hero {
      position:relative;z-index:2;padding:2rem;max-width:1100px;width:100%;
      animation:fadeIn 1.6s ease both;
    }
    .brand {
      font-family: 'Great Vibes', cursive;
      font-size:3.6rem;line-height:1;color:#fff;
      text-shadow:0 4px 14px rgba(0,0,0,0.35);
      margin-bottom:0.35rem;
    }
    .tagline{font-size:1.05rem;color:#fff;opacity:.95}

    /* --- Navigation --- */
    nav{background:#fffaf5;display:flex;justify-content:center;gap:1rem;padding:.9rem 1rem;position:sticky;top:0;z-index:50;box-shadow:0 2px 8px rgba(0,0,0,0.04)}
    nav a{padding:.45rem .9rem;border-radius:8px;font-weight:700;transition:all .18s}
    nav a:hover{background:#d97c5a;color:#fff}

    /* --- Sections --- */
    section{padding:3.5rem 1.5rem;max-width:1200px;margin:0 auto}
    h2{
      font-family:'Great Vibes',cursive;font-size:2.2rem;text-align:center;margin-bottom:1rem;color:#3f2f25;
    }
    h2::after{content:'';display:block;width:72px;height:3px;background:#d97c5a;margin:0.6rem auto 0;border-radius:3px}

    p.lead{max-width:900px;margin:0.6rem auto 0;text-align:center;color:#5b453c}

    /* --- Cards grid --- */
    .cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:1.25rem;margin-top:1.25rem}
    .card{background:#fff;border-radius:14px;overflow:hidden;box-shadow:0 8px 22px rgba(0,0,0,0.08);transition:transform .28s,box-shadow .28s}
    .card:hover{transform:translateY(-6px);box-shadow:0 18px 34px rgba(0,0,0,0.12)}
    .card img{width:100%;height:190px;object-fit:cover;display:block;transition:transform .5s}
    .card:hover img{transform:scale(1.05)}
    .card .card-body{padding:1rem}
    .card h3{font-family:'Great Vibes',cursive;font-size:1.25rem;margin-bottom:.2rem}
    .card p{color:#6b5349;font-size:.95rem}

    /* --- Slider --- */
    .slider{position:relative;border-radius:14px;overflow:hidden;margin-top:1rem;box-shadow:0 10px 30px rgba(0,0,0,0.08)}
    .slides{display:flex;transition:transform .5s ease}
    .slides img{width:100%;flex-shrink:0;height:420px;object-fit:cover}
    .slider-buttons{position:absolute;top:50%;left:0;right:0;display:flex;justify-content:space-between;padding:0 0.8rem;transform:translateY(-50%)}
    .slider-buttons button{background:rgba(255,255,255,0.85);border:0;padding:.5rem .85rem;border-radius:50%;cursor:pointer;font-size:1.3rem;transition:all .18s}
    .slider-buttons button:hover{background:#d97c5a;color:#fff}

    /* --- Contact --- */
    .contact-wrap{max-width:640px;margin:0 auto;position:relative}
    form{display:flex;flex-direction:column;gap:.9rem}
    input[type="text"],input[type="email"],textarea{
      padding:.85rem;border-radius:8px;border:1px solid #d9d1cc;font-size:1rem;background:#fff
    }
    textarea{min-height:140px;resize:vertical}
    .btn{background:#d97c5a;color:#fff;padding:.9rem 1rem;border:0;border-radius:8px;font-weight:700;cursor:pointer;transition:transform .12s,background .18s}
    .btn:hover{transform:scale(1.03);background:#c46544}

    .success-message{display:none;position:absolute;left:50%;transform:translateX(-50%);top:-42px;background:#3fb16f;color:#fff;padding:.5rem .9rem;border-radius:8px;box-shadow:0 6px 18px rgba(0,0,0,0.08)}

    /* --- Floating social --- */
    .social-fixed{position:fixed;right:12px;top:50%;transform:translateY(-50%);display:flex;flex-direction:column;gap:.5rem;z-index:60}
    .social-fixed a{width:44px;height:44px;background:#d97c5a;color:#fff;border-radius:8px;display:flex;align-items:center;justify-content:center;font-weight:700;box-shadow:0 6px 18px rgba(0,0,0,0.08)}
    .social-fixed a:hover{background:#c46544}

    /* --- Footer --- */
    footer{text-align:center;padding:2rem 1rem;background:#fffaf5;color:#3f2f25;font-size:.95rem}

    /* --- Responsiveness --- */
    @media (max-width:880px){ .slides img{height:260px} }
    @media (max-width:520px){
      header{height:56vh}
      .brand{font-size:2.6rem}
      .slides img{height:180px}
    }

    /* --- Animations --- */
    @keyframes fadeIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="hero" role="banner" aria-label="The Cacao Bloom hero">
      <div class="brand">The Cacao Bloom</div>
      <div class="tagline">Sweet carts & artisanal delights for your weddings, communions and celebrations</div>
    </div>
  </header>

  <!-- Nav -->
  <nav role="navigation" aria-label="Main navigation">
    <a href="#about">About</a>
    <a href="#products">Products</a>
    <a href="#events">Events</a>
    <a href="#gallery">Gallery</a>
    <a href="#contact">Contact</a>
  </nav>

  <!-- About -->
  <section id="about" aria-labelledby="about-heading">
    <h2 id="about-heading">About</h2>
    <p class="lead">At The Cacao Bloom we create memorable dessert experiences: charming sweet carts and handcrafted treats — chocolate bars studded with irregular chopped nuts, ginger cookies, and more. We serve weddings, communions, birthdays and private events.</p>
  </section>

  <!-- Products -->
  <section id="products" aria-labelledby="products-heading">
    <h2 id="products-heading">Products</h2>
    <div class="cards" role="list">
      <article class="card" role="listitem" aria-label="Artisanal chocolate">
        <img data-src="https://images.unsplash.com/photo-1612197524955-3fc3c3fa9a0e?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Artisanal chocolate bar with nuts" class="lazy">
        <div class="card-body">
          <h3>Artisanal Chocolate</h3>
          <p>Single-origin chocolate bars with irregularly chopped nuts — handmade and packaged beautifully for gifts or event favors.</p>
        </div>
      </article>

      <article class="card" role="listitem" aria-label="Ginger cookies">
        <img data-src="https://images.unsplash.com/photo-1606851093764-0cf8599c2991?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Handmade ginger cookies" class="lazy">
        <div class="card-body">
          <h3>Ginger Cookies</h3>
          <p>Warm, spiced ginger cookies baked in small batches — perfect as party treats or packaged favors.</p>
        </div>
      </article>

      <article class="card" role="listitem" aria-label="Sweet carts">
        <img data-src="https://images.unsplash.com/photo-1604917864204-6e9aee4501ee?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Decorated sweet cart at an event" class="lazy">
        <div class="card-body">
          <h3>Sweet Carts</h3>
          <p>Customizable candy and dessert carts for weddings, communions and birthdays — tailored to your theme and guest count.</p>
        </div>
      </article>
    </div>
  </section>

  <!-- Events -->
  <section id="events" aria-labelledby="events-heading">
    <h2 id="events-heading">Events</h2>
    <p class="lead">We provide full-service dessert experiences tailored to the event type. Pick a cart style, choose your treats, and we’ll handle delivery, setup and styling.</p>
    <div class="cards" style="margin-top:1rem">
      <div class="card"><img data-src="https://images.unsplash.com/photo-1508739773434-c26b3d09e071?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Wedding dessert setup" class="lazy"><div class="card-body"><h3>Weddings</h3></div></div>
      <div class="card"><img data-src="https://images.unsplash.com/photo-1600185368251-3c4dc5fa48e0?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Communion party desserts" class="lazy"><div class="card-body"><h3>Communions</h3></div></div>
      <div class="card"><img data-src="https://images.unsplash.com/photo-1589758438368-1d0e7f9c6b30?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=900" alt="Birthday sweets table" class="lazy"><div class="card-body"><h3>Birthdays</h3></div></div>
    </div>
  </section>

  <!-- Gallery / Slider -->
  <section id="gallery" aria-labelledby="gallery-heading">
    <h2 id="gallery-heading">Gallery</h2>
    <div class="slider" aria-roledescription="carousel">
      <div class="slides" id="slides">
        <img data-src="https://images.unsplash.com/photo-1606788075761-72d60d1ebcf3?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1400" alt="Event dessert display 1" class="lazy">
        <img data-src="https://images.unsplash.com/photo-1599785209707-27beec04c6a1?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1400" alt="Event dessert display 2" class="lazy">
        <img data-src="https://images.unsplash.com/photo-1612197524955-3fc3c3fa9a0e?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&w=1400" alt="Event dessert display 3" class="lazy">
      </div>

      <div class="slider-buttons" aria-hidden="false">
        <button id="prev" aria-label="Previous slide">&#10094;</button>
        <button id="next" aria-label="Next slide">&#10095;</button>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" aria-labelledby="contact-heading">
    <h2 id="contact-heading">Contact</h2>
    <p class="lead">Get in touch to request a quote, check availability or ask about custom flavors and styling.</p>

    <div class="contact-wrap">
      <div class="success-message" id="successMessage" role="status" aria-live="polite">Message sent — thank you!</div>

      <!-- Replace YOUR_FORMSPREE_ID with your actual Formspree endpoint (e.g. https://formspree.io/f/abcd1234) -->
      <form id="contactForm" action="https://formspree.io/f/YOUR_FORMSPREE_ID" method="POST" novalidate>
        <input type="text" name="name" placeholder="Your name" required aria-label="Name">
        <input type="email" name="email" placeholder="Email address" required aria-label="Email address">
        <textarea name="message" placeholder="Tell us about your event (date, location, guest count, message)" required aria-label="Message"></textarea>
        <button class="btn" type="submit">Send message</button>
      </form>
    </div>
  </section>

  <!-- Floating social links -- replace with your pages -->
  <div class="social-fixed" aria-hidden="false">
    <a href="https://instagram.com/yourprofile" target="_blank" rel="noopener noreferrer" aria-label="Instagram">IG</a>
    <a href="https://facebook.com/yourpage" target="_blank" rel="noopener noreferrer" aria-label="Facebook">FB</a>
  </div>

  <footer role="contentinfo">
    <p>&copy; <span id="year"></span> The Cacao Bloom. All rights reserved.</p>
  </footer>

  <script>
    // Update copyright year
    document.getElementById('year').textContent = new Date().getFullYear();

    /* -----------------------
       Lazy loading images
       ----------------------- */
    document.addEventListener('DOMContentLoaded', () => {
      const lazyImgs = document.querySelectorAll('img.lazy');
      if('IntersectionObserver' in window) {
        const obs = new IntersectionObserver((entries, observer) => {
          entries.forEach(entry => {
            if(entry.isIntersecting){
              const img = entry.target;
              img.src = img.dataset.src;
              img.removeAttribute('data-src');
              img.classList.remove('lazy');
              observer.unobserve(img);
            }
          });
        }, { rootMargin: '200px' });
        lazyImgs.forEach(img => obs.observe(img));
      } else {
        // Fallback: load all
        lazyImgs.forEach(img => { img.src = img.dataset.src; img.classList.remove('lazy'); });
      }
    });

    /* -----------------------
       Slider (basic)
       ----------------------- */
    (function(){
      const slidesEl = document.getElementById('slides');
      const slideImgs = slidesEl.querySelectorAll('img');
      const prevBtn = document.getElementById('prev');
      const nextBtn = document.getElementById('next');
      let idx = 0;

      function show(i){
        if(i < 0) idx = slideImgs.length - 1;
        else if(i >= slideImgs.length) idx = 0;
        else idx = i;
        slidesEl.style.transform = `translateX(-${idx * 100}%)`;
      }

      prevBtn.addEventListener('click', () => show(idx - 1));
      nextBtn.addEventListener('click', () => show(idx + 1));
      // keyboard
      document.addEventListener('keydown', e => {
        if(e.key === 'ArrowLeft') show(idx - 1);
        if(e.key === 'ArrowRight') show(idx + 1);
      });
      // autoplay
      setInterval(() => show(idx + 1), 5000);
    })();

    /* -----------------------
       Contact form: submit via Formspree + success UI
       ----------------------- */
    (function(){
      const form = document.getElementById('contactForm');
      const successMsg = document.getElementById('successMessage');
      form.addEventListener('submit', function(e){
        e.preventDefault();
        const endpoint = form.action;
        const data = new FormData(form);

        fetch(endpoint, {
          method: 'POST',
          body: data,
          headers: { 'Accept': 'application/json' }
        })
        .then(resp => {
          if(resp.ok){
            successMsg.style.display = 'block';
            form.reset();
            setTimeout(()=> successMsg.style.display = 'none', 5000);
          } else {
            resp.json().then(json => {
              alert((json && json.error) ? json.error : 'Error sending message — please try again.');
            }).catch(()=> alert('Error sending message — please try again.'));
          }
        }).catch(()=> {
          alert('Network error — please check your connection and try again.');
        });
      });
    })();
  </script>
</body>
</html>
